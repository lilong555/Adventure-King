【提交摘要】
增加角色调试场景，改进角色基础类和受击逻辑，修复精灵帧加载断言，并将新文件加入工程。

【变更目的（Why）】
- 提供一个独立的调试场景（`DebugScene`），用于快速测试角色功能（受击、攻击、升级、HP/MP 显示等），提高开发调试效率。
- 改进 `CharacterBase` 的受击与初始化流程：引入 `DamageInfo`、完善 `takeDamage`（暴击、护甲穿透、减伤、浮动与保底伤害），并添加对普通图片文件初始化的支持与精灵帧存在性检查以避免断言崩溃。
- 修改 `PlayerCharacter` 以在无法通过精灵帧创建时回退到普通文件加载，提升容错。
- 在 `HomeScene` 添加进入调试场景的入口按钮，方便从主界面快速访问。
- 将新源文件添加到 `Adventure-King.vcxproj`，并在本地通过 Debug 构建验证成功。

【变更内容（What） - 逐文件详述】
1) 新增：`Classes/DebugScene.h`、`Classes/DebugScene.cpp`
   - 实现一个用于调试的独立场景，包含：属性面板、HP/MP 条、伤害日志、按键/按钮触发的测试功能（受击、暴击、治疗、攻击、升级、重置、返回）。
   - 支持快捷键操作（1-5，R，ESC）。

2) 修改：`Classes/Character/CharacterBase.h`、`Classes/Character/CharacterBase.cpp`
   - 添加 `DamageInfo` 结构体定义（amount, penetration, isCritical, critMultiplier, attacker）。
   - 将 `takeDamage(float)` 改为 `takeDamage(const DamageInfo&)`，并实现：
     - 暴击倍率计算；
     - 护甲穿透与 MOBA 风格减伤公式（ARMOR_CONST/(ARMOR_CONST+def)）；
     - 5% 随机浮动；
     - 向下取整并保证最小伤害为 1；
     - 根据 `_maxHP` 的 5% 阈值决定是否切换到 HURT 状态。
   - 增加 `_maxHP` 成员以便阈值判断。
   - 新增 `initWithFile(const std::string&)` 方法以支持通过普通图片文件初始化角色精灵。
   - 修改 `initWithSpriteFrameName` 的实现：先通过 `SpriteFrameCache::getSpriteFrameByName` 检查帧是否存在，避免 `initWithSpriteFrameName` 触发断言；若找不到帧则返回 false 以便上层回退到 `initWithFile`。
   - 包含 `<cmath>`、`<cstdlib>`，并使用 `std::floor` 等。

3) 修改：`Classes/Character/PlayerCharacter.cpp`
   - `init(...)` 先尝试 `initWithSpriteFrameName`，若失败回退到 `initWithFile`。 保持角色属性初始化、状态机绑定逻辑不变。

4) 修改：`Classes/HomeScene.h`、`Classes/HomeScene.cpp`
   - 在主界面右上角添加角色调试菜单项，并实现 `menuDebugCallback` 以切换到 `DebugScene`。

5) 修改：`proj.win32/Adventure-King.vcxproj`
   - 把 `DebugScene.cpp` 与 `DebugScene.h` 添加到项目的 `ClCompile` / `ClInclude` 节点，确保在 Visual Studio 中参与编译与索引。

【测试/验证（How tested）】
- 使用提供的 Visual Studio/MSBuild 任务进行了 Debug 构建，构建成功（0 errors, 0 warnings）。
- 修复了运行时因 `Sprite::initWithSpriteFrameName` 找不到帧导致的断言崩溃，改为先检查帧存在性。
- 在本地运行流程中能打开 `DebugScene`、创建玩家（若精灵帧缺失会回退到文件加载占位），并能触发受击/治疗等按钮逻辑（请在运行时确保相应资源路径正确）。

【可能的注意事项与后续行动（Notes / Next steps）】
- 资源：`DebugScene` 使用的图片路径（例如 `Sprites/Characters/Player/Klee/spr_klee_run.png`）需存在于 `Resources` 中或使用精灵帧表（plist）并在程序启动前加载到 `SpriteFrameCache`。否则会回退到 `initWithFile`（普通图片）或使用占位符绘制。
- 代码风格：此次改动在若干文件加入了新的 include 与成员变量，建议在 review 时确认成员初始化策略（例如在 constructor 中初始化 `_maxHP`）。
- 单元/集成测试：可考虑为 `takeDamage` 提供独立的单元测试，覆盖暴击、穿透、极端防御等场景以确保数值边界正确。

【提交由来（Who）】
- Author: 自动化提交（由开发者代理）

【关联分支】
- 当前分支: `Optimize_and_add_character_classes`

--- 结束 ---
