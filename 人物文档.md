《冒险王》游戏角色系统结构文档

本文档详细描述了游戏内角色（Character）的系统架构、文件结构、核心类、数据结构和组件功能。

1. 角色系统文件结构

所有与角色系统相关的核心代码都位于 Classes/Character/ 目录下，其结构清晰地反映了组件化的设计思想。

Classes/
└── Character/
├── CharacterBase.h // 所有角色的基类，定义通用接口和组件容器
├── CharacterBase.cpp
├── PlayerCharacter.h // 玩家角色的实现，处理装备、升级等特有逻辑
├── PlayerCharacter.cpp
├── CharacterData.h // 核心数据结构和枚举（如属性、状态、装备类型）
│
└── components/ // 存放所有功能组件的子目录
├── AttributeComponent.h // 属性组件，负责所有数值计算
├── AttributeComponent.cpp
├── StateMachineComponent.h // 状态机组件，管理角色状态与动画
├── StateMachineComponent.cpp
├── SkillComponent.h // 技能组件，管理技能的学习、装备和使用
└── SkillComponent.cpp

- CharacterData.h: 整个系统的“字典”，定义了所有共享的数据类型，是系统解耦的关键。
- CharacterBase.h: 系统的“骨架”，定义了角色的核心概念和通用行为。
- PlayerCharacter.h: 基于骨架的“具体实现”，添加了玩家专属的功能。
- components/目录: 系统的“器官”，每个组件都是一个独立的功能单元，可以被 CharacterBase 组合使用。

2. 整体架构概述

游戏的角色系统采用了一种现代、灵活的 组件化架构（Component-Based Architecture）。其核心思想是：

- 实体-组件系统 (ECS) 的简化实现:
  - 一个核心的基类 CharacterBase 作为“实体（Entity）”，负责提供角色的基本身份和生命周期管理。
  - 将不同的功能模块（如属性、状态机、技能）分离成独立的“组件（Component）”。
  - CharacterBase 持有并管理这些组件的实例，通过组合不同的组件来构建出功能完整的角色。
- 优点:
  - 高内聚，低耦合: 每个组件只负责自己的逻辑（例如，AttributeComponent 只关心数值计算），使得代码更易于维护和扩展。
  - 复用性强: 例如，AttributeComponent 和 StateMachineComponent 可以被玩家、怪物或者 NPC 复用。
  - 灵活性高: 可以通过添加、移除或替换组件来轻松改变角色的行为，而无需修改核心类。

3. 核心类 UML 关系图

classDiagram
class CharacterBase {
-AttributeComponent \_attributeComponent
-StateMachineComponent \_stateMachineComponent
-SkillComponent \_skillComponent
+attack()\*
+takeDamage(DamageInfo)
+die()
+getAttributeComponent()
+getStateMachineComponent()
+getSkillComponent()
}

      class PlayerCharacter {
          +equip(Equipment)
          +unequip(EquipmentSlot)
          +addExperience(int)
          +levelUp()
          +useSkill(int)
      }

      class AttributeComponent {
          -Attributes _baseAttributes
          -Attributes _equipmentBonus
          -Attributes _finalAttributes
          +recalculateFinalAttributes()
          +getAttributeValue(AttributeType)
      }

      class StateMachineComponent {
          -CharacterState _currentState
          +changeState(CharacterState)
          +registerStateAnimation(CharacterState, string)
      }

      class SkillComponent {
          -vector~ActiveSkill~ _activeSlots
          +learnSkill(Skill)
          +equipActiveSkill(ActiveSkill, int)
          +useActiveSkill(int)
      }

      cocos2d.Sprite <|-- CharacterBase
      CharacterBase <|-- PlayerCharacter
      CharacterBase "1" *-- "1" AttributeComponent
      CharacterBase "1" *-- "1" StateMachineComponent
      CharacterBase "1" *-- "1" SkillComponent

4. 核心类详解

4.1 CharacterBase

- 文件: Character/CharacterBase.h
- 职责: 所有角色（包括玩家、敌人、NPC）的通用基类。
- 继承: 继承自 cocos2d::Sprite，意味着每个角色本身就是一个可以在场景中显示的精灵。
- 核心功能:

  - 组件容器: 持有 AttributeComponent, StateMachineComponent, SkillComponent 三个核心组件的
    std::unique_ptr，负责它们的生命周期。
  - 核心接口: 定义了所有角色都必须具备的通用行为接口，如：
    - attack(): 纯虚函数，强制子类（如 PlayerCharacter）实现自己的攻击逻辑。
    - takeDamage(const DamageInfo& info): 处理受击逻辑，计算最终伤害。
    - die(): 处理死亡逻辑。
  - 基础属性: 管理角色的 HP、MP、等级和经验值等基本状态。

  4.2 PlayerCharacter

- 文件: Character/PlayerCharacter.h
- 职责: 玩家可操作角色的具体实现。
- 继承: 继承自 CharacterBase。
- 核心功能:
  - 玩家特有逻辑: 实现了 CharacterBase 的 attack() 接口，并增加了玩家独有的逻辑，如：
    - addExperience() 和 levelUp(): 经验获取与升级逻辑。
    - equip() 和 unequip(): 装备穿戴和卸载的管理。
    - useSkill(): 使用技能的顶层接口，内部会调用 SkillComponent 来执行。
  - 数据初始化: 根据不同的职业（CharacterRole）初始化角色的基础属性。
  - 装备联动: 能够根据装备的武器（Weapon）来改变攻击动画和攻击力等参数。

5. 组件系统详解

5.1 AttributeComponent (属性组件)

- 文件: Character/components/AttributeComponent.h
- 职责: 角色所有战斗数值的计算中心。
- 核心功能:

  - 分层属性计算: 将属性分为多个来源，最终相加得出最终值：
    i. \_baseAttributes: 角色的基础属性（由等级、职业决定）。
    ii. \_equipmentBonus: 所有已穿戴装备提供的属性加成。
    iii. \_passiveSkillBonus: 所有已装备的被动技能提供的属性加成。
    iv. \_statusBonus: 来自各种状态效果（Buff/Debuff）的临时属性加成。
  - 最终属性: 调用 recalculateFinalAttributes() 会将上述所有属性叠加，存入
    \_finalAttributes，游戏中的所有伤害、防御计算都应基于这个最终值。
  - 状态效果管理: 负责管理角色身上的所有状态效果（StatusEffectInstance），并按帧更新它们的持续时间。

  5.2 StateMachineComponent (状态机组件)

- 文件: Character/components/StateMachineComponent.h
- 职责: 管理角色的状态（如站立、行走、攻击）及其对应的动画表现。
- 核心功能:

  - 状态切换: 通过 changeState(CharacterState newState) 方法来切换角色的当前状态。
  - 动画绑定: 使用 registerStateAnimation() 可以将一个 CharacterState 枚举与一个具体的动画名称（存储在 AnimationCache
    中）绑定。
  - 自动播放: 当状态切换时，会自动查找并播放与新状态绑定的动画。

  5.3 SkillComponent (技能组件)

- 文件: Character/components/SkillComponent.h
- 职责: 管理角色的所有技能，包括学习、装备和使用。
- 核心功能:
  - 技能库与槽位:
    - \_learnedSkills: 角色学会的所有技能列表。
    - \_activeSlots / \_passiveSlots: 玩家装备到快捷栏的主动/被动技能。
  - 技能使用: useActiveSkill() 负责检查并执行主动技能，包括处理法力消耗和冷却时间。
  - 冷却管理: update(float dt) 方法每帧被调用，用于更新所有主动技能的冷却倒计时。
  - 被动技能处理: 当被动技能被装备或卸下时，它会自动向 AttributeComponent 添加或移除对应的属性加成。

6. 核心数据结构 (CharacterData.h)

该文件是整个角色系统的数据字典，定义了所有相关的枚举和数据结构。

- 核心枚举:
  - CharacterRole: 职业（战士、法师等）。
  - CharacterState: 角色状态（待机、行走、攻击等）。
  - AttributeType: 属性类型（力量、防御、最大 HP 等）。
  - EquipmentSlot: 装备槽位（武器、头盔等）。
- 核心结构体:
  - Attributes: 一个 map 容器，用于存储一组属性（AttributeType 到 float 的映射），并重载了 + 和 +=
    运算符，方便属性的合并计算。
  - Equipment / Weapon: 装备和武器的数据结构，包含属性加成、装备槽位等信息。
  - Skill / ActiveSkill / PassiveSkill:
    技能的基类和派生类，定义了技能的公共属性以及主动/被动技能的特有属性（如冷却、蓝耗、属性加成）。
  - DamageInfo: 伤害信息包，在攻击时创建，包含了攻击力、是否暴击、攻击者等信息，用于伤害计算。
